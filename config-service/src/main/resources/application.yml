server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git: #default
          uri: http://github.com/joneconsulting/spring-cloud-config
          default-label: master
